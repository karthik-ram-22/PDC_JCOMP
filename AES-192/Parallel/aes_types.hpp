
#ifndef TYPES_H
#define TYPES_H

#include <string>

// Types Byte (8 bits), Word (16 bits) and DWord (32 bits)

typedef unsigned short int word;
typedef unsigned long dword;

string msg_final_mark;

#endif
